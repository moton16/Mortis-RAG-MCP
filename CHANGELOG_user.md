# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[Semantic Versioning](https://semver.org/lang/zh-CN/)。提交信息为 Conventional Commits。

## [0.7.0] - 2026-09-13

> **升级须知**：完全向后兼容现有的知识库与历史缓存。文档解析功能默认保持关闭，仅在需要时按需开启，既有 Markdown 检索习惯与配置完全不受影响。

### Added

- **PDF 与 Office 文档检索支持（按需开启）**：
  - 支持将知识库内的 PDF、Word、PPT、Excel 与图片等文档自动解析为 Markdown 并纳入搜索。
  - 转换产物自动保存在库内专用的 `.mortis-parsed/` 文件夹中，绝不改动或污染原有文件；搜索时可直接按原目录路径正常检索到它们。
  - 支持查看待解析列表、一键后台提交解析与查询进度。
  - 默认为关闭状态，如需使用只需在配置文件中填入 MinerU Token 并开启即可。
- **知识库描述与精准定向搜索**：
  - 注册或管理知识库时，可为知识库添加一句话内容描述（如“数电教材与复习课件”）。
  - AI 在检索时能够根据你的问题智能选择最相关的知识库，大幅减少无关库的干扰，让回答更快、更精准。
  - 跨多个库搜索时，会自动提示建议指定具体知识库或目录，帮助获得更精准的结果。

### Changed & Improved

- **表格排版与阅读体验优化**：
  - 复杂表格不再会被生硬切断，表头与整体结构完整保留，检索结果中查看表格更清晰。
  - 规整小表格自动整理为美观整齐的 Markdown 格式。
- **性能与稳定性提升**：
  - 大幅优化了海量文件库的初始扫描与日常同步速度，彻底告别卡顿。
  - 增强了多客户端同时使用时的系统稳定性与异常任务自愈能力。

## [0.6.0] - 2026-09-02

> **升级须知（重要变更）**：部分工具名称精简更名。`kb_unregister` 更名为 `kb_remove`（移除知识库）；`kb_vaults` 更名为 `kb_list`（查看知识库列表）；原 `kb_list` 更名为 `kb_list_files`（列出文件）。

### Added

- **独立私密库（solo vault）**：
  - 新增独立库功能（`kb_init_solo`），可把任意文件夹注册为独立知识库。
  - 独立库平时不参与跨库全局搜索，只有在明确指定要搜索该库时才会查询，非常适合存放个人私密日记或临时测试内容。
  - 普通库与独立库可随时相互转换，已建好的索引与缓存完整保留，转换瞬间完成。
- **日常同步速度飞跃**：
  - 大幅提升了日常对账同步速度，未修改的文件直接秒级跳过，海量笔记库无需反复读取磁盘，日常同步毫无感知。
- **代码笔记搜索体验优化**：
  - 笔记中代码块里的 `#` 注释不再会被误识别为标题，保证代码结构完整。
  - 优化了短英文缩写（如 AI、OS 等）的搜索准确度，既能精准找到专业缩写，又不会误中无关单词。
- **多客户端并发稳定加固**：
  - 加强了多软件、多窗口同时使用知识库时的安全性，避免配置文件意外损坏。

### Changed

- **工具命名规范化**：
  - 统一了管理工具的命名逻辑，工具操作更直观。
- **配套使用指南升级**：
  - 同步更新了使用文档与 AI 交互指南，引导更准确。

## [0.5.0] - 2026-08-30

> **升级须知**：平滑升级，无需重新生成索引，现有缓存无缝兼容。

### Added

- **高级过滤与分页检索**：
  - 搜索支持按文件夹目录前缀限定范围（例如只搜索“工作项目/”目录下的笔记）。
  - 支持按笔记标签（Tags）与最后修改时间范围精准筛选。
  - 增加了分页功能，浏览海量搜索结果更自如。
- **搜索结果自动去重**：
  - 自动识别并过滤完全重复的内容段落，备份副本不再挤占前排搜索结果。
- **知识库重要度权重与分组展示**：
  - 支持为不同的知识库设置权重（例如让“正式文档”权重大于“杂记库”），搜索时优先展示重要库的内容。
  - 跨库搜索支持按知识库分组呈现，条理更清晰。
- **索引快照一键迁移（换机免重新索引）**：
  - 支持将建好的知识库索引一键打包导出为压缩包。换新电脑或换目录时直接导入即可使用，完全不需要重新联网调用 API 计算，省时省力。
- **Windows 原生实时文件监听**：
  - 智能感知文件变动，修改笔记后毫秒级自动更新索引，平时完全不占用系统后台资源。
- **网络波动自动重试与断点续做**：
  - 遇到网络波动或服务商限流时会自动排队重试；如遇异常文件会自动记录，随时可在状态中查看并在网络恢复后自动补全。
- **图片图注搜索（可选开启）**：
  - 支持将笔记中的图片描述与图注文字纳入搜索范围。

## [0.4.1] - 2026-08-30

### Changed & Improved

- **安装与部署文档体验优化**：
  - 完善了各种 Python 环境下的安装指引，明确运行时零外部依赖，帮助新手快速排错完成部署。
- **配套使用指南更新**：
  - 补充了混合检索使用技巧与配置说明。

## [0.4.0] - 2026-08-30

### Added

- **全新“语义+关键词”混合检索**：
  - 搜索能力全面升级：将自然语言语义理解与精准关键词匹配深度融合。
  - 显著提升了简短中文词汇、人名、专有名词的搜索准确率，搜得更全、更准。
- **超低内存运行模式（可选）**：
  - 支持将索引数据直接保存在本地数据库中，使服务后台常驻内存占用几乎降为零，特别适合长时间在个人电脑后台挂机运行。
- **检索状态信息扩展**：
  - 状态查询中新增了当前检索模式与存储引擎的展示。

## [0.3.0] - 2026-08-30

### Added

- **多知识库管理与一键注册**：
  - 支持将电脑上的任意文件夹注册为独立知识库，电脑重启后依然生效。
  - 支持注册、注销知识库，并可随时查看已注册的全部知识库列表与状态。
- **跨库全局搜索**：
  - 单次搜索可自动覆盖所有已注册的知识库，并在结果中清晰标注来源自哪个知识库。
- **统一灵活的配置系统**：
  - 支持全局默认配置与自定义配置文件，多设备间迁移配置更加简便。

### Changed

- **项目品牌升级**：
  - 正式更名为 **Mortis'RAG MCP**，全面优化代码开源脱敏与分发文档。
  - 配套发布官方使用教程与快速上手指南。

## [0.1.0] / [0.2.0] - 2026-08-04 ~ 2026-08-08

### Added

- **核心检索与阅读服务**：
  - 基于 MCP 协议的标准知识库检索服务，供主流 AI 助手直接连接调用。
  - 自动将长篇 Markdown 笔记智能分块并建立本地索引，支持自然语言检索与原文阅读。
  - 内置快速精排模型，自动把最相关的笔记内容排在最前。
- **增量同步与本地缓存**：
  - 本地持久化缓存索引，未变动的文件不重复计算，省电省流量。
  - 支持离线纯本地模式与免联网哈希检索。
- **私密笔记排除管理**：
  - 支持通过标签或排除规则设置隐私笔记，防止草稿或私密日记被 AI 检索到。
