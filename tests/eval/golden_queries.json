{
  "说明": "金标准查询集。expect = 期望命中的 source 子串（库内相对路径）。path_prefix/vault_path 可选，用于验证定向检索。vault 支持相对路径（相对当前 JSON 文件路径）。",
  "queries": [
    {
      "vault": "./vault",
      "query": "TTL 与非门的噪声容限怎么算",
      "expect": "数电/",
      "path_prefix": "",
      "note": "概念型查询，期望命中数电笔记子树"
    }
  ]
}
